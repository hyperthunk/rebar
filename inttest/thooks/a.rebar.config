{before_hooks, [
    {clean, "echo preclean >> a.preclean.out"},
    {compile, erlc, "touch a.precompile.erlc.out"}
]}.

{after_hooks, [
    {clean, "echo postclean >> a.postclean.out"},
    {compile, erlc, "touch a.postcompile.erlc.out"}
]}.
